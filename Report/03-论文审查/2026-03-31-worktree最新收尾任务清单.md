# 2026-03-31 worktree 最新收尾任务清单

适用目录：`/Users/yanxi/code/Latex/ucas-master-restructure`  
适用分支：`thesis-restructure-task1`  
适用对象：当前 8 章正文、4 个附录、现有图资产与送审前统稿收尾

## 一、当前判断

当前 worktree 已经完成最重的结构性重构，论文主线已经从“单章堆叠优化过程”改成：

1. 第 5 章负责优化问题建模、方案筛选与正文路标。
2. 第 6 章负责执行期与启动期的代表性局部案例。
3. 第 7 章负责 LoongArch64 最小可用 LSX 浮点 SIMD 路径这一全文主结论。

因此，后续重点不再是继续大拆章节，而是做最后一轮论文级收口：

1. 收术语和文风。
2. 收附录口径与旧命名。
3. 收结论章与全稿口径一致性。
4. 补没画完的图和伪代码。
5. 统一选定终稿图版本并清理旧资产。
6. 做最终编译、目检与送审前检查。

## 二、已完成的大项

1. 8 章正文结构已经落地，`Tex/Mainmatter.tex` 已切到 `Chap_Selection`、`Chap_Cases`、`Chap_SIMD`。
2. 第 4 章已经重写为“实验设计与正确性验证”，并引入 `Snapshot-A` 到 `Snapshot-E`。
3. 附录已经拆成 `App_Candidates`、`App_Intrinsic`、`App_SIMD_Ablation`、`App_DexVerifier`。
4. 三个正式方案的原理性介绍明显增强，第 7 章已经补入 LSX 启用伪代码和 `IR -> LSX` 映射表。
5. 当前 PDF 可正常生成，worktree 下 `Tmp/Thesis.pdf` 已达到可阅读状态。

## 三、剩余任务总表

### T1. 术语与文风收口

目标：继续降低技术报告味、模板味和 AI 味，使文字更像学位论文正文。

重点处理：

1. 压低 `闭环`、`口径`、`可复核`、`门控` 这类高频词的密度。
2. 减少“需要说明的是”“可以得到”“后续”这类偏套话的句法。
3. 拆分仍然过长的解释句，优先改成“现象 -> 原因 -> 结论”的短句。

优先文件：

1. `Tex/Chap_Porting.tex`
2. `Tex/Chap_Verification.tex`
3. `Tex/Chap_Selection.tex`
4. `Tex/App_Candidates.tex`

### T2. 附录 A 的口径和旧命名清理

目标：把“候选方案归档”彻底从旧“failed-schemes”命名中剥离出来。

必须处理：

1. 删除 `Tex/App_Candidates.tex` 中“它们并不被称为失败方案”这类反向写法，直接改成正向学术表述。
2. 将图名、图注、标签从 `failed-schemes-heatmap` 改成更合适的候选方案命名。
3. 如有必要，同步重命名图片文件，避免仓库里继续保留 `failed-*` 资产。

相关文件：

1. `Tex/App_Candidates.tex`
2. `Img/mybenchmark/failed-schemes-heatmap.pdf`
3. `Img/mybenchmark/failed-schemes-heatmap.png`
4. `Img/failed-schemes-heatmap.pdf`

### T3. 结论章与新结构再同步

目标：让第 8 章完全跟上 5/6/7 章的新职责边界。

必须处理：

1. 删除或改写“附录中的补充实验不参与正文第 5 章主结论比较”这类旧结构表述。
2. 把结论中的“第 5 章统一比较”改成与“第 5 章筛选、第 6 章案例、第 7 章主结论”一致的分层总结。
3. 让结论更明确地区分：
   - 第 5 章：筛选与路标
   - 第 6 章：局部案例
   - 第 7 章：全文最强结论

优先文件：

1. `Tex/Chap_Conclusion.tex`
2. 必要时回看 `Tex/Chap_Intro.tex`

### T4. 还没画的图与伪代码

这些项目是当前真正还没补齐的“空缺项”。

#### T4.1 还没画的图

1. 第 1 章研究主线图  
   作用：在绪论直接交代“问题 -> 方案 -> 证据 -> 结论 -> 章节承接”。
2. 第 2 章“LoongArch 架构约束 -> 三类优化问题”映射图  
   作用：把第 2 章和第 6、7 章真正挂钩。
3. 第 6.3 节 `DexFileVerifier` 主热点阶段链机制图  
   作用：补上目前只有收益图、缺少机制图的问题。
4. 第 8 章结论层级总结图  
   作用：用一张总结图收束“局部案例 + 主结论 + 适用边界”。

说明：第 8 章总结图优先级低于前三项，但如果目标是送审前完成一轮高质量补图，建议一并补上。

#### T4.2 还没补的伪代码/机制性表达

1. 第 6.1 节 baseline JIT 提前触发判定逻辑  
   当前问题：机理图有了，但“进入条件如何判断”仍缺一个更学术化的伪代码表达。
2. 第 6.2 节字符串热路径批量搬移核心循环框架  
   当前问题：已有机制图，但还缺一段能够说明“为什么不是简单 memcpy”的核心伪代码。
3. 第 6.3 节 `DexFileVerifier` 阶段链推进顺序  
   当前问题：阶段收益图已经有，但还缺“阶段链如何逐段推进”的伪代码或三线表式表达。

### T5. 已有图的终稿化与重绘

这些图已经存在，但仍应按“论文终稿图”标准再做一轮统一提升。

#### P0：必须优先处理

1. `Img/verification/experiment-validation-workflow.pdf`
2. `Img/optimization/candidate-schemes-screening-map.pdf`
3. `Img/optimization/scheme1-tiered-jit-flow.pdf`
4. `Img/optimization/scheme2-string-copy-paths.pdf`
5. `Img/optimization/scheme3-vectorization-boundary.pdf`
6. `Img/optimization/scheme3-lsx-lowering.pdf`

#### P1：在 P0 之后统一提升

1. `Img/porting/art-porting-overview.pdf`
2. `Img/porting/art-porting-stages.pdf`
3. `Img/porting/art-porting-runtime-paths.pdf`
4. `Img/mybenchmark/dexfileverifier-phase-chain-speedup.pdf`
5. `Img/mybenchmark/statistical-validation-stripplot.pdf`
6. `Img/mybenchmark/final-comparison-heatmap.pdf`

### T6. 图版本与旧资产清理

目标：在补图完成后，收掉仓库里的重复图和临时版本。

必须判断：

1. `scheme3-lsx-lowering.pdf` 与 `scheme3-lsx-lowering-v5.pdf` 哪个作为正文最终版本。
2. 一旦正文引用固定，删除未采用的重复版本，避免后续再次混用。
3. 如果 `failed-schemes-heatmap` 被改名，也要同步删除旧名资产。

相关资产：

1. `Img/optimization/scheme3-lsx-lowering.pdf`
2. `Img/optimization/scheme3-lsx-lowering.png`
3. `Img/optimization/scheme3-lsx-lowering.svg`
4. `Img/optimization/scheme3-lsx-lowering-v5.pdf`
5. `Img/optimization/scheme3-lsx-lowering-v5.png`
6. `Img/optimization/scheme3-lsx-lowering-v5.svg`

### T7. 旧文件与结构残留清理

目标：避免重构完成后仍保留容易误导的旧入口或旧中间状态。

建议处理：

1. 评估 `Tex/Chap_Optimization.tex` 是否继续保留为占位记录文件。
2. 如果保留，需确保其不会被误重新并入正文。
3. 如果不再需要，可在确认无引用后删除。

### T8. 送审前最终检查

最后一轮必须做的检查如下：

1. 全量编译一次，确认日志无新错误和引用断裂。
2. 通读第 1、4、5、6、7、8 章，检查章节衔接是否自然。
3. 逐张目检关键图，确认没有重叠、遮挡、拥挤、字号失衡。
4. 检查图注和正文首次引用是否匹配。
5. 检查术语是否统一：
   - `Snapshot-A/B/C/D/E`
   - 候选方向 / 未保留方向 / 对比方案
   - LSX / SIMD / lowering / workload / baseline
6. 检查附录是否只承接细节，不反向压过正文主线。

## 四、建议执行顺序

### 第一阶段：先收正文口径

1. 做 T1 术语与文风收口。
2. 做 T2 附录 A 的旧命名清理。
3. 做 T3 结论章同步。

### 第二阶段：补空缺图与伪代码

1. 先补第 1 章主线图。
2. 再补第 2 章问题映射图。
3. 再补第 6.3 节机制图。
4. 接着补第 6.1、6.2、6.3 三个伪代码/三线表表达。
5. 如时间允许，补第 8 章总结图。

### 第三阶段：统一重绘与清理

1. 按 P0/P1 顺序重绘现有关键图。
2. 选定最终图版本，清理重复图和旧命名图。
3. 处理 `Tex/Chap_Optimization.tex` 这类旧文件残留。

### 第四阶段：最终检查

1. 完整编译。
2. 通篇目检。
3. 做最后一轮语言精修。

## 五、当前最关键的四件事

如果只按最短路径收尾，优先级应固定为：

1. 收掉附录 A 和全稿中的旧口径、旧命名。
2. 同步第 8 章，让结论完全匹配新结构。
3. 补第 1 章主线图、第 2 章映射图、第 6.3 节机制图。
4. 补第 6.1、6.2、6.3 的机制性伪代码。

## 六、结论

当前 worktree 的状态不是“还没重构完”，而是“主结构已经完成，进入最后一轮论文级收尾”。  
剩余任务的本质也不是继续扩写材料，而是把已经成形的正文进一步打磨成更像送审稿的状态：

1. 语言更克制。
2. 图示更完整。
3. 主线更稳定。
4. 旧命名和旧痕迹更少。
5. 全稿边界更干净。
